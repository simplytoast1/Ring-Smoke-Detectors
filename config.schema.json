{
  "pluginAlias": "RingSmokeDetectors",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "customUiPath": "./dist/homebridge-ui",
  "headerDisplay": "Log in with your Ring account to discover and manage your Kidde smoke detectors. See the [GitHub page](https://github.com/simplytoast1/Ring-Smoke-Detectors) for more info.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Ring Smoke Detectors",
        "description": "Exposes Kidde/Ring smart smoke and CO detectors to HomeKit via Homebridge"
      },
      "refreshToken": {
        "title": "Refresh Token",
        "type": "string",
        "description": "Generated automatically when you log in via the settings UI"
      },
      "locationIds": {
        "title": "Location IDs",
        "type": "array",
        "items": { "type": "string" },
        "description": "Optional: limit to specific Ring location IDs"
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "default": false
      }
    },
    "required": ["refreshToken"]
  }
}
